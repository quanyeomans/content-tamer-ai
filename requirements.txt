# Core dependencies
pypdf>=6.0.0
tiktoken>=0.11.0
tqdm>=4.67.1
requests>=2.32.5
pymupdf>=1.26.3
pillow>=11.3.0
pytesseract>=0.3.13
pdfid>=1.1.0
rich>=13.7.0
defusedxml>=0.7.1              # For secure XML parsing (XXE protection)

# AI Provider dependencies (install based on your needs)
# openai>=1.0.0              # For OpenAI models (recommended)
# anthropic>=0.34.0          # For Claude models (Opus 4.1, Sonnet 4, Claude 3.5 series)  
# google-genai>=0.7.0        # For Gemini models (2.5 Pro/Flash, 2.0 Flash/Pro - new unified SDK)

# Local LLM dependencies (for offline processing)
psutil>=5.9.0               # For system hardware detection
# ollama                    # Ollama client library (optional - HTTP requests used by default)

# Post-processing organization dependencies
spacy>=3.7.0                # Enhanced NLP for document classification (Phase 1)
python-dateutil>=2.8.0     # Date parsing and temporal analysis (Phase 1)
sentence-transformers>=2.2.0  # State-of-the-art embeddings for ML refinement (Phase 2)
scikit-learn>=1.3.0         # Machine learning algorithms for clustering (Phase 2)

# Development dependencies (optional)
# pytest>=8.4.1              # For running tests
# pytest-bdd>=6.0.0          # For BDD testing framework
# cucumber-tag-expressions>=4.0.0  # For BDD scenario tagging